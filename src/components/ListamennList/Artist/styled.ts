import styled from 'styled-components'

export const ItemWrap = styled.div`
  p {
    margin: 0.5rem 0;
  }
  .exhibition-wrap {
    padding-left: 1rem;
    display: grid;
    p {
      color: gray;
      font-size: 0.8rem;
      margin: 0;
    }
  }
`